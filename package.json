{
  "name": "ricehub",
  "private": true,
  "description": "Tooling for the RiceHub browser extension. The extension itself is described in README.md.",
  "scripts": {
    "test": "node --test"
  }
}
